{
    // primitive,non primitive data types

    // # primitive data type

    // string
    const idName: string = "Emam Bokhari";

    // number
    const idNo: number = 2111;

    // boolearn
    const isActive: boolean = true;

    // null 
    const profileStatus: null = null;

    // undefined
    const x: undefined = undefined;

    // any data type , any type ar data type a js ar moto dynamic types ar data rakha jai
    let y: any = "20";
    y = 30;
    y = true;
    y = null;
    y = undefined;


    // # Non primitive datat type

    // array
    const friendsList: string[] = ["Abdul Jalil", "Abdul Malik", "Abdur Rahman"];

    const numberOfProducts: number[] = [2, 4, 5, 6, 11, 21];

    // tuple
    const coordinate: [number, number] = [1, 5];

    const info: [string, number, string] = ["Abdul Mannan", 24, "Bangladesh"];



}